{
  "buildCommand": "cd .. && pnpm install && cd 06_ADMIN_PANEL && pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "echo 'installed in build step'",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
