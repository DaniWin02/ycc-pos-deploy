{
  "buildCommand": "cd .. && pnpm install && cd 05_KDS_SYSTEM && pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "echo 'installed in build step'",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
